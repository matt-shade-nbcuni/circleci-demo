version: 2.1

### Executors
executors:
  node:
    docker: 
      - image: circleci/node:latest

### Jobs
jobs:
  build: 
    executor:
      name: node
    resource_class: small
    working_directory: /tmp
    steps:
      - checkout

### Workflows
workflows: 
  version: 2.1
  build_test_deploy: 
    jobs: 
      - build