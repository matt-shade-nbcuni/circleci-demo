---
    apiVersion: eksctl.io/v1alpha5
    kind: ClusterConfig
    
    metadata:
      name: plant-demo
      region: us-east-2
      version: '1.18'  
    managedNodeGroups:
    - name: linux-nodes
      launchTemplate:
        id: lt-id
        version: "1"